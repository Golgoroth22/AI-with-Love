# FAQ: Функции и возможности

## Семантический поиск в документах

### Как работает семантический поиск?

Семантический поиск использует AI для понимания смысла вашего запроса и поиска релевантных фрагментов в проиндексированных документах.

**Отличие от обычного поиска:**
- **Обычный поиск:** ищет точное совпадение слов
- **Семантический:** понимает смысл и находит похожие по смыслу фрагменты

**Пример:**
```
Запрос: "как войти в систему"
Найдёт фрагменты про: login, authentication, вход в аккаунт, авторизация
```

**Преимущества:**
- Понимает синонимы и похожие понятия
- Работает на русском и английском языках
- Учитывает контекст запроса

---

### Как индексировать документы?

**Через экран Ollama:**

1. Откройте экран "Ollama" в главном меню
2. Выберите тип документа:
   - **Текст:** для вставки текста вручную
   - **PDF:** для загрузки PDF-файлов

**Для текста:**
1. Введите или вставьте текст в поле
2. Нажмите "Обработать текст"
3. Дождитесь сообщения "Документ сохранён"

**Для PDF:**
1. Нажмите "Выбрать PDF"
2. Выберите файл на устройстве
3. Дождитесь обработки (зависит от размера файла)

**Поддерживаемые форматы:**
- ✅ Текст (.txt, вводимый вручную)
- ✅ PDF (.pdf)
- ✅ Markdown (.md)

**Ограничения:**
- Максимальный размер PDF: 50 MB (рекомендуется до 10 MB)
- Обработка больших файлов может занять несколько минут

---

### Как настроить порог поиска (threshold)?

Порог определяет минимальное совпадение для результатов поиска.

**Где настроить:**
На экране чата есть слайдер "Порог поиска" (обычно в верхней части или в настройках).

**Значения:**
- **0.3-0.5:** Очень мягкий поиск, много результатов (может быть шум)
- **0.6-0.7:** ⭐ Рекомендуемое значение, баланс точности и полноты
- **0.8-0.95:** Очень строгий поиск, только точные совпадения

**Когда изменять:**
- 📉 Мало результатов → уменьшите порог до 0.5
- 📈 Много нерелевантных результатов → увеличьте порог до 0.75

**Пример:**
```
Запрос: "проблемы с авторизацией"
Порог 0.4: находит 15 документов (включая общие о безопасности)
Порог 0.7: находит 3 документа (только о проблемах входа)
```

---

### Почему поиск не находит документы?

**Проверьте:**

1. **Документы проиндексированы?**
   - Перейдите в Ollama → должны быть записи "Документ сохранён"
   - Проверьте количество документов в базе

2. **Сервер RAG включён?**
   - В чате нажмите на иконку гаечного ключа (🔧)
   - Включите "RAG Server"

3. **Ollama запущен?**
   - Локальная установка: откройте терминал и выполните `ollama --version`
   - Должно показать версию, например "ollama version 0.1.20"

4. **Порог поиска не слишком высокий?**
   - Попробуйте снизить до 0.5 или 0.4

**Если всё настроено:**
- Попробуйте переформулировать запрос
- Используйте ключевые слова: "найди в документах", "что говорится о"
- Убедитесь, что документы содержат информацию по вашему запросу

---

## GitHub интеграция

### Как настроить GitHub Assistant?

**Шаг 1: Создайте Personal Access Token**

1. Откройте GitHub → Settings → Developer settings
2. Personal access tokens → Tokens (classic)
3. "Generate new token" → "Generate new token (classic)"
4. Выберите scopes:
   - ✅ `repo` (доступ к репозиториям)
   - ✅ `read:packages`
   - ✅ `read:org`
5. Нажмите "Generate token"
6. **ВАЖНО:** Скопируйте токен сразу! Он больше не отобразится.

**Шаг 2: Добавьте токен в приложение**

1. Откройте файл `SecureData.kt` в проекте
2. Вставьте токен:
   ```kotlin
   val githubPersonalAccessToken = "ghp_ваш_токен_здесь"
   ```
3. Сохраните файл

**Шаг 3: Включите сервер**

1. В чате нажмите иконку гаечного ключа (🔧)
2. Включите переключатель "GitHub Assistant"

**Важно:** Не коммитьте токен в git! Держите `SecureData.kt` в `.gitignore`.

---

### Доступные GitHub операции

**Репозитории:**
```
"GitWithLove покажи репозиторий facebook/react"
```
Информация: описание, звёзды, форки, язык, лицензия

**Поиск кода:**
```
"GitWithLove найди код Compose в android/compose-samples"
```
Ищет по всем файлам репозитория с использованием GitHub Code Search

**Issues:**
```
"GitWithLove создать issue: Bug in authentication"
"GitWithLove список issues в repo-name"
```
Создание и просмотр issues

**Коммиты:**
```
"GitWithLove последние 5 коммитов в owner/repo"
```
История коммитов с авторами и датами

**Code Review:**
```
"ReviewPR проверь PR #12"
```
AI анализирует код на ошибки, стиль, безопасность

---

### Примеры использования GitHub

**Исследование проекта:**
```
GitWithLove покажи информацию о репозитории vercel/next.js
```

**Поиск примеров кода:**
```
GitWithLove найди примеры использования useState в facebook/react
```

**Работа с issues:**
```
GitWithLove список открытых issues в Golgoroth22/AI-with-Love
```

**Code Review:**
```
ReviewPR сделай ревью для PR #45 в AI-with-Love
```

---

## MCP инструменты

### Что такое MCP?

**MCP (Model Context Protocol)** - протокол для подключения внешних инструментов к AI.

**Идея:** AI может вызывать внешние функции (tools) для получения информации или выполнения действий.

**Доступные серверы:**
- 🌐 **RAG Server:** семантический поиск в документах
- 🐙 **GitHub Assistant:** работа с GitHub
- 🌿 **Local Git:** локальные git операции
- 🎫 **Support Assistant:** управление тикетами поддержки

**Как включить:**
1. В чате нажмите на иконку гаечного ключа (🔧)
2. Переключите нужные серверы
3. Значок с цифрой показывает сколько серверов активно

---

### RAG Server

**Инструменты:**
- `semantic_search` - поиск в документах

**Использование:**
```
"Найди в документах информацию о настройке Ollama"
```

**Когда включать:**
- Когда нужен поиск по проиндексированным документам
- Для ответов на основе вашей базы знаний

---

### GitHub Assistant

**Инструменты:**
- `get_repo` - информация о репозитории
- `search_code` - поиск кода
- `create_issue` - создание issue
- `list_issues` - список issues
- `list_commits` - история коммитов
- `get_repo_content` - содержимое файла
- `get_pull_request` - информация о PR
- `get_pr_files` - файлы в PR

**Использование:**
```
"GitWithLove покажи последние коммиты в AI-with-Love"
```

**Требования:**
- GitHub Personal Access Token в `SecureData.kt`
- Включённый сервер в настройках MCP

---

### Local Git

**Инструменты:**
- `git_status` - статус репозитория
- `git_branch` - список веток
- `git_diff` - изменения в файлах
- `git_pr_status` - статус PR

**Использование:**
```
"GitLocal покажи статус репозитория"
"GitLocal какие файлы изменены?"
```

**Требования:**
- Локальный git репозиторий
- Путь к репозиторию настроен в конфиге

---

### Support Assistant

**Инструменты:**
- `get_ticket` - информация о тикете
- `list_user_tickets` - список тикетов пользователя
- `create_ticket` - создание тикета
- `update_ticket` - обновление статуса

**Использование:**
```
"Support покажи тикет #123"
"Support создай тикет: проблема с поиском"
```

---

## Как создать свой MCP инструмент?

**Для разработчиков:**

См. документацию в `CLAUDE.md` раздел "Adding a New MCP Tool".

**Кратко:**
1. Добавьте tool definition в `http_mcp_server.py`
2. Реализуйте handler функцию
3. Добавьте tool builder в `ChatViewModel.kt`
4. Зарегистрируйте в `executeAgenticToolCall()`
5. Обновите `McpServerConfig.kt` если нужен новый сервер

---

## Локальная обработка документов

### Как работает локальная обработка?

**День 21 обновление:** Документы теперь обрабатываются **локально** на вашей машине!

**Преимущества:**
- ⚡ **7x быстрее** (45s vs 320s для 15KB файла)
- ✅ Нет таймаутов для больших файлов
- 🔒 Данные не покидают ваше устройство
- 💰 Экономия ресурсов сервера

**Архитектура:**
```
Документ → Извлечение текста (PDFBox)
         → Чанкирование (1000 символов, 200 overlap)
         → Локальные эмбеддинги (Ollama: nomic-embed-text)
         → Отправка на сервер (только эмбеддинги)
         → Сохранение в SQLite
```

**Требования:**
- Ollama установлен и запущен
- Модель `nomic-embed-text` загружена

---

### Настройка Ollama

**Установка:**

**macOS:**
```bash
brew install ollama
```

**Linux:**
```bash
curl https://ollama.ai/install.sh | sh
```

**Windows:**
Скачайте с https://ollama.ai

**Запуск:**
```bash
# Запустить Ollama сервис
ollama serve

# В другом терминале - загрузить модель
ollama pull nomic-embed-text

# Проверить статус
ollama list
```

---

### Производительность

**Скорость обработки:**
- 1-2 секунды на чанк (500-1000 символов)
- 15KB текста (~15 чанков) = 30-60 секунд

**Оптимизация:**
- Используйте GPU если доступен
- Уменьшите размер чанков для быстрой обработки
- Обрабатывайте документы по частям (по 5-10 страниц)

**Проблемы:**
- Медленная обработка → проверьте использует ли Ollama GPU
- Таймауты → уменьшите размер файла или chunk_size

---

## Полнотекстовый поиск vs Семантический

### В чём разница?

**Полнотекстовый поиск:**
- Ищет точное совпадение слов
- Быстрый, но не понимает смысл
- Пример: "login" не найдёт "authentication"

**Семантический поиск:**
- Понимает смысл запроса
- Находит похожие концепции
- Пример: "login" найдёт "authentication", "войти", "авторизация"

**Когда использовать:**
- 🔍 Полнотекстовый: точный поиск по ключевым словам
- 🧠 Семантический: поиск по смыслу и контексту

---

## Управление документами

### Как удалить документы из базы?

**Через SQLite:**
```bash
# Резервная копия
cp server/data/embeddings.db server/data/embeddings.db.backup

# Удалить старые записи (старше 30 дней)
sqlite3 server/data/embeddings.db \
  "DELETE FROM documents WHERE created_at < datetime('now', '-30 days');"

# Удалить по имени файла
sqlite3 server/data/embeddings.db \
  "DELETE FROM documents WHERE source_file = 'old_doc.pdf';"
```

**Через код (для разработчиков):**
Реализуйте MCP tool `delete_document` в `http_mcp_server.py`

---

### Как посмотреть что проиндексировано?

**SQL запрос:**
```bash
sqlite3 server/data/embeddings.db \
  "SELECT source_file, COUNT(*) as chunks FROM documents GROUP BY source_file;"
```

**Результат:**
```
faq_authentication.md|15
guide.pdf|42
troubleshooting.md|8
```

---

## Интеграция с другими сервисами

### Можно ли подключить другие AI модели?

**Для эмбеддингов:**
Сейчас используется `nomic-embed-text` через Ollama. Можно заменить на:
- OpenAI embeddings (требует API key)
- Cohere embeddings
- HuggingFace модели

**Для генерации ответов:**
Используется Perplexity Agentic API. Можно заменить на:
- OpenAI GPT-4
- Anthropic Claude
- Google Gemini

**Изменение:** Редактируйте `OLLAMA_API_URL` в `http_mcp_server.py`

---

## Советы по эффективному использованию

### Лучшие практики

**Для семантического поиска:**
1. ✅ Формулируйте запросы как вопросы или описания проблемы
2. ✅ Используйте ключевые слова "найди в документах", "что говорится о"
3. ✅ Начинайте с порога 0.6, корректируйте по результатам
4. ✅ Индексируйте документы с ясной структурой (заголовки, разделы)

**Для GitHub интеграции:**
1. ✅ Используйте ключевое слово "GitWithLove" для активации
2. ✅ Указывайте полное имя репозитория (owner/repo)
3. ✅ Для code review используйте "ReviewPR #номер"

**Для Support Assistant:**
1. ✅ Используйте "Support" для активации
2. ✅ Указывайте номер тикета: "Support #123"
3. ✅ Описывайте проблему подробно при создании тикета

---

## Контакты

Если у вас есть вопросы или предложения:

📧 **Email:** support@aiwithlove.example.com
🎫 **Тикет:** "Support создай тикет: [ваш вопрос]"
📚 **Документация:** CLAUDE.md, README.md
