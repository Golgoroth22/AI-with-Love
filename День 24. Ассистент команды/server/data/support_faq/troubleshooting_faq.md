# FAQ: Решение проблем

## Ошибки при загрузке PDF

### Приложение вылетает при загрузке PDF

**Симптомы:**
- При выборе PDF файла приложение закрывается
- Ошибка "Out of memory"
- Зависание на этапе "Обработка..."

**Причина:**
Файл слишком большой для обработки в памяти устройства.

**Решение:**

1. **Проверьте размер файла:**
   - Должен быть до 10-15 MB
   - Для файлов 15-50 MB обработка может быть нестабильной

2. **Если файл больше 15 MB:**
   - Разбейте PDF на части:
     - Онлайн-сервисы: ilovepdf.com, smallpdf.com
     - Или загружайте по 10-15 страниц за раз
   - Уменьшите качество изображений в PDF
   - Используйте оптимизированный PDF (File → Save As → Reduce File Size)

3. **Увеличьте лимит памяти (для разработчиков):**
   ```kotlin
   // В AndroidManifest.xml
   <application android:largeHeap="true">
   ```

**Для очень больших файлов:**
Используйте серверную обработку (если настроена) или обрабатывайте локально через CLI.

---

### PDF обработан, но ничего не нашлось

**Проверьте:**

1. **PDF содержит текст, а не только изображения?**
   - Попробуйте скопировать текст из PDF в другом приложении
   - Если не копируется → PDF состоит из изображений
   - **Решение:** Нужно OCR (распознавание текста)

2. **Язык документа соответствует модели?**
   - Модель `nomic-embed-text` лучше работает с английским и русским
   - Для других языков результаты могут быть хуже

3. **Порог поиска не слишком высокий?**
   - Попробуйте снизить до 0.4 или 0.3
   - Проверьте что документ действительно содержит релевантную информацию

4. **Документ корректно разбит на чанки?**
   - Проверьте в базе данных:
   ```bash
   sqlite3 server/data/embeddings.db \
     "SELECT content FROM documents WHERE source_file = 'your_file.pdf' LIMIT 1;"
   ```

**OCR для сканированных PDF:**
```bash
# Используйте Tesseract или онлайн-сервисы
tesseract input.pdf output.pdf pdf
```

---

### Ошибка при извлечении текста из PDF

**Ошибка:** "Failed to extract text from PDF"

**Причины:**
- PDF защищён паролем
- PDF повреждён
- PDF имеет специальную защиту от копирования

**Решение:**
1. **Снимите защиту:**
   - Используйте пароль если знаете
   - Онлайн-сервисы для снятия защиты (на свой риск!)

2. **Проверьте целостность:**
   - Откройте PDF в другом приложении
   - Попробуйте пересохранить: File → Save As

3. **Попробуйте другой PDF reader:**
   - Экспортируйте из Adobe Reader
   - Используйте PDF-XChange или Foxit

---

## Проблемы с Ollama

### Ollama не запускается

**Проверка статуса:**

**macOS:**
```bash
# Проверить установку
ollama --version

# Проверить запущен ли
ps aux | grep ollama

# Перезапустить
brew services restart ollama

# Или вручную
ollama serve
```

**Linux:**
```bash
# Проверить статус службы
systemctl status ollama

# Перезапустить
sudo systemctl restart ollama

# Логи
journalctl -u ollama -f
```

**Windows:**
```powershell
# Проверить процесс
Get-Process ollama

# Перезапустить службу
Restart-Service Ollama
```

**Если Ollama не установлен:**
```bash
# macOS
brew install ollama

# Linux
curl https://ollama.ai/install.sh | sh

# Windows: скачайте с https://ollama.ai
```

---

### Модель не найдена

**Ошибка:** "model 'nomic-embed-text' not found"

**Решение:**

1. **Скачайте модель:**
   ```bash
   ollama pull nomic-embed-text
   ```

2. **Проверьте установку:**
   ```bash
   ollama list
   ```
   Должно показать:
   ```
   NAME                  ID            SIZE
   nomic-embed-text      <hash>        274MB
   ```

3. **Если модель скачана, но не работает:**
   ```bash
   # Переустановите модель
   ollama rm nomic-embed-text
   ollama pull nomic-embed-text
   ```

4. **Проверьте работу:**
   ```bash
   ollama run nomic-embed-text "test"
   ```

---

### Медленная обработка документов

**Симптомы:**
- Обработка одного чанка занимает >5 секунд
- CPU нагружен на 100%
- Вентиляторы работают на максимум

**Причины:**
- Слабое железо (CPU/GPU)
- Ollama использует CPU вместо GPU
- Большой размер документа
- Много параллельных процессов

**Оптимизация:**

1. **Включите GPU ускорение:**
   ```bash
   # Проверьте доступ к GPU
   ollama run nomic-embed-text --verbose

   # Для NVIDIA GPU установите CUDA
   # Для AMD GPU установите ROCm
   # Для Apple Silicon GPU уже используется
   ```

2. **Уменьшите размер чанков:**
   - В `OllamaScreen` измените `chunk_size` с 1000 на 500
   - Меньше данных → быстрее обработка

3. **Обрабатывайте по частям:**
   - Разбейте большой документ на части по 5-10 страниц
   - Загружайте и обрабатывайте по очереди

4. **Увеличьте количество workers:**
   ```python
   # В http_mcp_server.py
   max_workers = 8  # Было 4
   ```

**Нормальная скорость:**
- 1-2 секунды на chunk (500-1000 символов)
- 15KB текста (~15 chunks) = 30-60 секунд
- С GPU: в 2-3 раза быстрее

---

### Ошибка подключения к Ollama

**Ошибка:** "Failed to connect to Ollama: Connection refused"

**Причины:**
- Ollama не запущен
- Неправильный порт
- Firewall блокирует соединение

**Решение:**

1. **Проверьте запущен ли Ollama:**
   ```bash
   # Должен ответить
   curl http://localhost:11434
   ```

2. **Запустите Ollama:**
   ```bash
   ollama serve
   ```

3. **Проверьте порт:**
   - По умолчанию: 11434
   - В `http_mcp_server.py` проверьте `OLLAMA_API_URL`

4. **Firewall:**
   - Разрешите порт 11434
   - macOS: System Preferences → Security → Firewall
   - Windows: Windows Defender → разрешить порт

---

## Сетевые ошибки

### Connection timeout / Cannot connect to server

**Симптомы:**
- "Connection timed out"
- "Unable to connect to MCP server"
- Запрос висит бесконечно

**Диагностика:**

1. **Проверьте сервер запущен:**
   ```bash
   cd server
   python3 http_mcp_server.py
   ```
   Должно показать:
   ```
   🚀 MCP HTTP Server - Local Mode with Ollama & GitHub
   Server: http://0.0.0.0:8080
   ```

2. **Проверьте IP адрес в `ServerConfig.kt`:**
   - **Эмулятор Android:** `10.0.2.2:8080`
   - **Реальное устройство:** `<IP вашего компьютера>:8080`

3. **Узнайте IP компьютера:**
   ```bash
   # macOS/Linux
   ifconfig | grep "inet "

   # Windows
   ipconfig
   ```
   Используйте локальный IP (обычно 192.168.x.x)

4. **Проверьте firewall:**
   - **Windows:** разрешите Python в Firewall
   - **macOS:** System Preferences → Security → Firewall → разрешите входящие соединения
   - **Linux:**
   ```bash
   sudo ufw allow 8080
   ```

**Тест подключения:**
```bash
# С компьютера
curl http://localhost:8080

# С устройства (замените IP)
curl http://192.168.1.100:8080
```

---

### Rate limit exceeded (GitHub API)

**Ошибка:** "API rate limit exceeded for..."

**Причина:**
GitHub ограничивает количество запросов:
- **Без токена:** 60 запросов/час
- **С токеном:** 5000 запросов/час

**Решение:**

1. **Настройте Personal Access Token:**
   - См. FAQ "GitHub интеграция"
   - Токен увеличивает лимит до 5000/час

2. **Проверьте текущий лимит:**
   ```bash
   curl -H "Authorization: Bearer YOUR_TOKEN" \
     https://api.github.com/rate_limit
   ```
   Результат:
   ```json
   {
     "rate": {
       "limit": 5000,
       "remaining": 4999,
       "reset": 1677649200
     }
   }
   ```

3. **Подождите сброса:**
   - Лимит сбрасывается каждый час
   - Время сброса указано в `reset` (Unix timestamp)

4. **Уменьшите частоту запросов:**
   - Не делайте много запросов подряд
   - Используйте кэш для повторяющихся запросов

---

### SSL/TLS ошибки

**Ошибка:** "SSL: CERTIFICATE_VERIFY_FAILED"

**Причины:**
- Устаревшие SSL сертификаты
- Проблемы с системным временем
- Антивирус или корпоративный прокси

**Решение:**

1. **Проверьте системное время:**
   - Время должно быть точным
   - Включите автоматическую синхронизацию

2. **Обновите SSL сертификаты:**
   ```bash
   # macOS
   brew update && brew upgrade ca-certificates

   # Linux (Debian/Ubuntu)
   sudo apt update && sudo apt install ca-certificates
   ```

3. **Для разработки (НЕ для продакшена):**
   ```python
   # В http_mcp_server.py
   import ssl
   ssl._create_default_https_context = ssl._create_unverified_context
   ```

---

## Проблемы с производительностью

### Приложение медленно работает

**Симптомы:**
- Долгая загрузка
- Лагает интерфейс
- Долгие ответы AI

**Оптимизация:**

1. **Очистите старые сообщения:**
   - Долгая история чата замедляет работу
   - Функция автосжатия активируется каждые 5 сообщений
   - Вручную: Настройки → Очистить историю

2. **Отключите неиспользуемые MCP серверы:**
   - Каждый активный сервер добавляет инструменты в контекст
   - Включайте только нужные

3. **Уменьшите количество результатов поиска:**
   - В `semantic_search`: `limit=3` вместо 5
   - Меньше данных → быстрее обработка

4. **Проверьте размер базы данных:**
   ```bash
   sqlite3 server/data/embeddings.db "SELECT COUNT(*) FROM documents;"
   ```
   Если >10000 записей - база большая, рассмотрите очистку

5. **Оптимизируйте базу данных:**
   ```bash
   sqlite3 server/data/embeddings.db "VACUUM;"
   sqlite3 server/data/embeddings.db "REINDEX;"
   ```

---

### Большое потребление памяти

**Симптомы:**
- Приложение использует >500 MB RAM
- OOM (Out of Memory) ошибки
- Устройство тормозит

**Причины:**
- Много проиндексированных документов в памяти
- Большие PDF файлы
- Утечки памяти (редко)

**Решение:**

1. **Перезапустите приложение:**
   - Force stop → Открыть заново

2. **Очистите кэш:**
   - Настройки → Приложения → AI with Love → Очистить кэш

3. **Удалите старые документы:**
   ```bash
   # Резервная копия
   cp server/data/embeddings.db server/data/embeddings.db.backup

   # Удалить старые записи (старше 30 дней)
   sqlite3 server/data/embeddings.db \
     "DELETE FROM documents WHERE created_at < datetime('now', '-30 days');"
   ```

4. **Для разработчиков:**
   - Проверьте утечки с помощью Android Profiler
   - Используйте `WeakReference` для больших объектов

---

## Ошибки токенов (AI API)

### "Insufficient tokens" / "Token limit exceeded"

**Причина:**
Слишком длинный контекст для AI модели (история чата + инструменты + документы).

**Объяснение:**
AI модели имеют лимит на количество токенов (слов):
- **gpt-4-turbo:** 128K токенов (~96K слов)
- **gpt-3.5-turbo:** 16K токенов (~12K слов)
- **~750 слов** ≈ 1000 токенов

**Решение:**

1. **Автоматическое сжатие диалога:**
   - Происходит автоматически каждые 5 сообщений
   - Старые сообщения сжимаются в краткое резюме

2. **Очистите чат вручную:**
   - Долгое нажатие на сообщение → "Очистить историю"
   - Или в настройках → Очистить чат

3. **Уменьшите количество инструментов:**
   - Отключите неиспользуемые MCP серверы
   - Каждый инструмент занимает токены

4. **Уменьшите результаты `semantic_search`:**
   - Используйте `limit=2-3` вместо 5
   - Меньше документов → меньше токенов

**Проверка размера контекста:**
```kotlin
// В ChatViewModel логи показывают размер
logD("📤 Context size: ~${contextSize} tokens")
```

---

### Ошибки API ключа

**Ошибка:** "Invalid API key" или "Unauthorized"

**Причина:**
- API ключ неверный
- API ключ истёк
- API ключ не имеет нужных прав

**Решение:**

**Для Perplexity API:**
1. Проверьте ключ в `SecureData.kt`:
   ```kotlin
   val perplexityApiKey = "pplx-..."
   ```
2. Убедитесь что ключ действителен на https://www.perplexity.ai/settings/api

**Для GitHub:**
1. Проверьте токен в `SecureData.kt`:
   ```kotlin
   val githubPersonalAccessToken = "ghp_..."
   ```
2. Проверьте scopes: `repo`, `read:packages`, `read:org`
3. Токен может истечь - создайте новый

---

## Логи и отладка

### Где найти логи?

**Android Studio Logcat:**
1. Подключите устройство/эмулятор
2. Android Studio → Logcat (внизу экрана)
3. Фильтр: `package:com.example.aiwithlove`

**Фильтр по тегам:**
```
tag:ChatViewModel     # Основная логика чата
tag:McpClient         # MCP взаимодействие
tag:OllamaViewModel   # Индексация документов
```

**Поиск ошибок:**
```
level:error    # Только ошибки
level:warn     # Предупреждения
```

**Сервер логи:**
```bash
cd server
# Логи в реальном времени
python3 http_mcp_server.py

# Сохранить в файл
python3 http_mcp_server.py 2>&1 | tee server.log
```

---

### Как сообщить об ошибке?

**Соберите информацию:**

1. **Версия приложения:**
   - Настройки → О приложении → Версия

2. **Версия Android:**
   - Настройки устройства → О телефоне

3. **Что делали перед ошибкой:**
   - Пошаговое описание действий

4. **Текст ошибки:**
   - Скриншот или точный текст сообщения

5. **Логи (если возможно):**
   ```bash
   adb logcat > logs.txt
   # Или из Android Studio: Logcat → Export
   ```

**Создайте issue или тикет:**

**GitHub:**
```
GitWithLove создать issue в Golgoroth22/AI-with-Love:
Заголовок: [Bug] Краткое описание
Описание:
- Версия: 1.2.3
- Android: 13
- Шаги воспроизведения: ...
- Ожидаемое поведение: ...
- Фактическое поведение: ...
```

**Или через Support:**
```
Support создай тикет:
Проблема: [описание]
```

---

## Крайние случаи

### Полная переустановка

Если ничего не помогает:

1. **Сохраните данные:**
   - Экспортируйте важные документы
   - Сохраните историю чата (если нужна)

2. **Удалите приложение:**
   - Долгое нажатие → Удалить
   - Или: Настройки → Приложения → Удалить

3. **Очистите остатки:**
   ```bash
   adb shell pm clear com.example.aiwithlove
   ```

4. **Установите заново:**
   - Скачайте последнюю версию
   - Установите
   - Настройте заново (токены, серверы)

---

### Сброс базы данных

**На сервере:**
```bash
# Резервная копия
cp server/data/embeddings.db server/data/embeddings.db.backup

# Удалить базу
rm server/data/embeddings.db

# Пересоздать
cd server
python3 http_mcp_server.py
# База создастся автоматически
```

**В приложении:**
```kotlin
// Через код (для разработчиков)
chatRepository.clearAllMessages()
embeddingsRepository.clearAll()
```

---

## Контакты поддержки

Если проблема не решена:

📧 **Email:** support@aiwithlove.example.com
🎫 **Тикет:** "Support создай тикет: [описание проблемы]"
💬 **GitHub Issues:** https://github.com/Golgoroth22/AI-with-Love/issues
📚 **Документация:** README.md, CLAUDE.md, LOCAL_PROCESSING.md

**При обращении укажите:**
- Версию приложения и Android
- Шаги воспроизведения
- Скриншот ошибки
- Логи (если возможно)
