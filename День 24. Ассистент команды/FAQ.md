# FAQ - Часто Задаваемые Вопросы

## Какую LLM/модель ты используешь?

**Для генерации ответов:** Perplexity Agentic API (модель `openai/gpt-5-mini`)
**Для создания эмбеддингов:** Ollama с моделью `nomic-embed-text`
**Локальная обработка:** Документы обрабатываются на вашей машине через Ollama, затем сохраняются на сервер.

## 📚 Содержание

- [О проекте](#о-проекте)
- [Быстрый старт](#быстрый-старт)
- [Функциональность](#функциональность)
- [Настройка и конфигурация](#настройка-и-конфигурация)
- [Использование](#использование)
- [Архитектура](#архитектура)
- [Устранение неполадок](#устранение-неполадок)
- [Разработка](#разработка)

---

## О проекте

### Что такое "AI with Love"?

**AI with Love** - это образовательное Android-приложение, демонстрирующее современные паттерны агентивного AI с возможностями RAG (Retrieval-Augmented Generation) и интеграцией с внешними сервисами через протокол MCP (Model Context Protocol).

**Текущий день разработки**: День 24 - Ассистент команды (Team Assistant)

### Какие проблемы решает проект?

1. **Интеллектуальный поиск по документам** - семантический поиск вместо простого текстового
2. **Автоматизация работы с GitHub** - поиск кода, создание issues, анализ PR
3. **Автоматизация code review** - AI-анализ pull requests с выявлением проблем
4. **Ассистент технической поддержки** - автоматические ответы на вопросы пользователей с использованием базы знаний
5. **Управление задачами команды** - создание, назначение, приоритизация задач с умным распределением нагрузки
6. **Локальная обработка данных** - приватность и производительность

### Какие технологии использованы?

**Frontend (Android):**
- Kotlin + Jetpack Compose
- Coroutines + Flow для асинхронности
- Room Database для локального хранения
- Koin для dependency injection

**Backend (MCP Server):**
- Python 3.9+
- aiohttp для асинхронного HTTP
- SQLite для хранения эмбеддингов
- Ollama для генерации эмбеддингов

**AI & ML:**
- Perplexity Agentic API для генерации ответов
- Ollama + nomic-embed-text для эмбеддингов
- Семантический поиск на основе косинусного сходства

**Интеграции:**
- GitHub API (через официальный MCP server)
- Model Context Protocol (MCP) - протокол для инструментов

---

## Быстрый старт

### Минимальные требования

- **macOS/Linux/Windows** с установленным Python 3.9+
- **Android Studio** Hedgehog или новее
- **Ollama** установлен и запущен
- **Java 17+** для Android-разработки
- **Git** для работы с репозиторием

### Установка за 5 минут

#### 1. Клонируйте репозиторий

```bash
git clone https://github.com/Golgoroth22/AI-with-Love.git
cd "AI-with-Love/День 24. Ассистент команды"
```

#### 2. Установите Ollama

**macOS:**
```bash
brew install ollama
ollama serve
ollama pull nomic-embed-text
```

**Linux:**
```bash
curl https://ollama.ai/install.sh | sh
ollama serve
ollama pull nomic-embed-text
```

**Windows:**
Скачайте установщик с https://ollama.ai

#### 3. Запустите MCP Server

```bash
cd server
python3 http_mcp_server.py
```

Сервер запустится на `http://0.0.0.0:8080`

#### 4. Настройте API ключи

Создайте файл `app/src/main/java/com/example/aiwithlove/util/SecureData.kt`:

```kotlin
package com.example.aiwithlove.util

object SecureData {
    // Perplexity API Key (обязательно)
    val perplexityApiKey = "YOUR_PERPLEXITY_API_KEY"

    // GitHub Personal Access Token (для GitHub функций)
    val githubPersonalAccessToken = "ghp_YOUR_GITHUB_TOKEN"

    // Server configuration
    val serverIp = "10.0.2.2" // для эмулятора
}
```

#### 5. Соберите и запустите приложение

```bash
./gradlew assembleDebug
adb install app/build/outputs/apk/debug/app-debug.apk
```

---

## Функциональность

### Что умеет приложение?

#### 🔍 1. Семантический поиск в документах (RAG)

**Возможности:**
- Индексация текстовых документов и PDF
- Семантический поиск по смыслу, а не по словам
- Настройка порога релевантности (threshold)
- Цитирование источников в ответах
- Локальная обработка на вашей машине (приватность)

**Как работает:**
1. Документ разбивается на фрагменты (chunks) по 1000 символов
2. Для каждого фрагмента генерируется эмбеддинг через Ollama
3. Эмбеддинги сохраняются в локальную SQLite базу
4. При поиске вычисляется косинусное сходство с запросом
5. AI использует найденные фрагменты для формирования ответа

**Пример использования:**
```
Вопрос: "Как настроить двухфакторную аутентификацию?"
→ Система находит релевантные разделы в FAQ
→ AI формирует пошаговую инструкцию с цитатами
```

#### 🐙 2. GitHub интеграция

**Доступные операции:**

| Операция | Команда | Что делает |
|----------|---------|------------|
| **Информация о репозитории** | `GitWithLove покажи репозиторий facebook/react` | Звёзды, форки, описание, язык, лицензия |
| **Поиск кода** | `GitWithLove найди код useState в facebook/react` | Поиск по всем файлам репозитория |
| **Создание issue** | `GitWithLove создать issue: Bug in login` | Создание нового issue |
| **Список issues** | `GitWithLove список issues в repo-name` | Открытые/закрытые issues |
| **История коммитов** | `GitWithLove последние 5 коммитов` | Коммиты с авторами и датами |
| **Чтение файлов** | `GitWithLove покажи содержимое README.md` | Содержимое файла из репозитория |

**Настройка GitHub:**
1. Перейдите на https://github.com/settings/tokens
2. "Generate new token (classic)"
3. Выберите scopes: `repo`, `read:packages`, `read:org`
4. Скопируйте токен в `SecureData.kt`

#### 🔎 3. Автоматическое ревью кода (Code Review)

**Возможности:**
- Анализ Pull Request по номеру
- Проверка на security, bugs, performance, style
- Оценка качества кода (0-10)
- Конкретные рекомендации с номерами строк
- Интеграция с документацией проекта (через RAG)

**Категории анализа:**
- 🔴 **Security**: SQL injection, hardcoded secrets, unsafe API
- 🟠 **Bugs**: null checks, empty catch, force unwraps
- 🟡 **Performance**: nested loops, inefficient algorithms
- 🔵 **Style**: debug prints, TODOs, naming conventions

**Пример использования:**
```
ReviewPR сделай ревью для PR #45
→ AI анализирует все изменённые файлы
→ Выдаёт отчёт с оценкой и рекомендациями
```

**Формат отчёта:**
```markdown
# 📋 Code Review: PR #45

**Общая оценка**: 8/10 ⭐

**Проблемы:**
- 🔴 0 критических
- 🟠 1 серьёзная
- 🟡 3 незначительных

### Файл: ChatViewModel.kt
🟠 Line 425: Отсутствует обработка ошибок
Рекомендация: Используйте runAndCatch wrapper
```

#### 🎫 4. Ассистент технической поддержки (День 23)

**Возможности:**
- Просмотр тикетов из CRM
- Автоматический поиск решений в FAQ
- Создание новых тикетов
- Обновление статуса тикетов
- Комбинированные ответы: контекст тикета + база знаний

**Доступные операции:**

| Операция | Команда | Результат |
|----------|---------|-----------|
| **Просмотр тикета** | `Support покажи тикет #1` | Карточка с деталями тикета |
| **Список тикетов пользователя** | `Support тикеты пользователя 2` | Список всех тикетов |
| **Создание тикета** | `Support создай тикет: проблема с поиском` | Новый тикет в системе |
| **Обновление тикета** | `Support обнови тикет #1 статус resolved` | Изменение статуса |
| **Помощь по тикету** | `Support тикет #1 - помоги решить` | Тикет + поиск в FAQ |

**Статусы тикетов:**
- 🔵 `open` - Открыт
- 🟡 `in_progress` - В работе
- 🟢 `resolved` - Решён
- ⚪ `closed` - Закрыт

**Приоритеты:**
- ⬇️ `low` - Низкий
- ➡️ `medium` - Средний
- ⬆️ `high` - Высокий
- 🔴 `critical` - Критический

**Категории:**
- 🔐 `authentication` - Аутентификация
- ⚙️ `features` - Функции
- 🔧 `troubleshooting` - Устранение неполадок
- 📝 `other` - Прочее

**Пример workflow:**
```
Пользователь: "Support тикет #1 - почему не работает вход?"

→ 1. AI получает данные тикета (пользователь, проблема, история)
→ 2. AI ищет в FAQ решения по авторизации
→ 3. AI формирует ответ:
   - Контекст тикета (что происходит)
   - Возможные причины из базы знаний
   - Пошаговое решение
   - Ссылки на документацию
```

#### 👥 5. Ассистент команды - Управление задачами (День 24)

**Возможности:**
- **Форма создания задач** - быстрое создание через диалоговое окно с валидацией
- Создание задач с приоритетами и назначением исполнителей
- Умное распределение задач на основе загрузки команды
- Поиск похожих задач для предотвращения дубликатов
- Связывание задач с тикетами поддержки
- Отслеживание статуса и истории изменений
- RAG-рекомендации для приоритизации

**Два способа создания задач:**
1. **Через форму** (рекомендуется для быстрого создания):
   - Напишите "создай задачу" или "новая задача" в чате
   - Появится диалоговое окно с формой
   - Заполните поля: Название, Описание, Приоритет
   - Нажмите "Создать" - задача создаётся мгновенно

2. **Через AI** (для комплексных задач):
   - Опишите задачу в свободной форме
   - AI задаст уточняющие вопросы
   - После подтверждения создаст задачу с учётом контекста

**Ключевые слова для диалога создания задачи:**
- "создай задачу" / "create task"
- "новая задача" / "new task"
- "добавь задачу" / "add task"

При использовании этих фраз появляется форма с полями:
- **Название** (обязательно) - краткое описание задачи
- **Описание** (обязательно) - детальная информация
- **Приоритет** (обязательно) - выбор из выпадающего списка: 🔴 Высокий / 🟡 Средний / 🟢 Низкий

**Доступные операции:**

| Операция | Команда | Результат |
|----------|---------|-----------|
| **Создание задачи (форма)** | `Создай задачу` или `Создай задачу - Название` | Диалоговое окно с формой |
| **Создание задачи (AI)** | `Опиши задачу: исправить баг входа, high` | AI-ассистированное создание |
| **Список задач** | `Покажи задачи с приоритетом high` | Отфильтрованный список |
| **Назначение задачи** | `Назначь задачу #1 на developer_1` | Обновление assignee |
| **Обновление статуса** | `Задача #1 статус in_progress` | Изменение статуса |
| **Загрузка команды** | `Кто сейчас свободен?` | Список членов команды с workload |
| **Поиск похожих** | `Есть ли похожие задачи на баг 2FA?` | Семантический поиск |

**Приоритеты задач:**
- 🔴 `high` - Высокий (срочно)
- 🟡 `medium` - Средний (стандартный)
- 🟢 `low` - Низкий (когда будет время)

**Статусы задач:**
- 📝 `todo` - Ожидает выполнения
- ⚙️ `in_progress` - В работе
- ✅ `done` - Выполнена

**Автоматическое определение намерения:**

AI автоматически понимает, что вы хотите:
- **Задачи**: "задач", "приоритет", "назначить", "статус задач", "workload"
- **Поддержка**: "проблема", "тикет", "баг", "не работает"
- **Гибрид**: Оба типа ключевых слов = доступ ко всем инструментам

**Пример workflow - создание задачи через форму:**
```
Пользователь: "Создай задачу - Исправить баг с 2FA"

→ 1. Keyword detection: "создай задачу" обнаружено
→ 2. Извлечение названия: "Исправить баг с 2FA"
→ 3. Открытие формы с предзаполненным названием
→ 4. Пользователь заполняет:
   - Название: "Исправить баг с 2FA" (уже заполнено)
   - Описание: "Пользователи не могут войти через SMS код"
   - Приоритет: выбирает "🔴 Высокий"
→ 5. Кнопка "Создать" активна (все поля заполнены)
→ 6. Нажатие "Создать" → прямой вызов MCP create_task
→ 7. Ответ в чате:
   "✅ Задача #7 успешно создана!

   **Название:** Исправить баг с 2FA
   **Приоритет:** high
   **Описание:** Пользователи не могут войти через SMS код"
→ 8. TopAppBar обновляется: "• 5 задач 🔴 3"
```

**Пример workflow - создание задачи через AI:**
```
Пользователь: "Нужна задача на исправление бага с 2FA, high priority"

→ 1. Определение намерения: TASK_MANAGEMENT (нет точного ключевого слова)
→ 2. Поиск похожих задач (search_similar_tasks) - нет дубликатов
→ 3. Поиск документации (semantic_search) - найдена SECURITY.md
→ 4. Проверка загрузки (get_team_workload) - Bob свободен
→ 5. Создание задачи (create_task):
   - title: "Исправить баг с 2FA"
   - priority: "high"
   - assignee: "developer_2" (Bob)
→ 6. Ответ: "✅ Создана задача #7, назначена на Bob Smith.
            Найдена документация: SECURITY.md (2FA implementation)"
```

**Пример workflow - умное назначение:**
```
Пользователь: "Покажи задачи high и предложи кому назначить"

→ 1. Список задач (list_tasks: priority=high, status=todo)
→ 2. Загрузка команды (get_team_workload)
→ 3. RAG-поиск (semantic_search: "task prioritization guidelines")
→ 4. Ответ:
   "📋 Высокоприоритетные задачи:
    1. Task #1: Исправить баг 2FA (не назначена)
    2. Task #5: Оптимизация БД (Alice, загрузка: 3)

    💡 Рекомендация: Начните с Task #1 (критический баг).
    Можно назначить на Bob (Frontend Dev, загрузка: 1).
    См. guidelines: TASK_PRIORITY.md"
```

**Пример workflow - задача из тикета:**
```
Пользователь: "Создай задачу из тикета #12"

→ 1. Определение намерения: HYBRID
→ 2. Получение тикета (get_ticket: ticket_id=12)
→ 3. Поиск похожих задач (search_similar_tasks)
→ 4. Проверка загрузки (get_team_workload: role_filter="Backend")
→ 5. Создание задачи (create_task: related_ticket_id=12)
→ 6. Обновление тикета (update_ticket: note="Создана задача #15")
→ 7. Ответ: "✅ Создана задача #15, назначена на Alice Johnson.
            Тикет #12 обновлён со ссылкой."
```

**UI показывает:**
- **TopAppBar**: "Тикет #12 • 5 задач 🔴 2" (текущий тикет, активные задачи, high priority count)
- **Приветственное сообщение**: Объясняет возможности работы с задачами и тикетами
- **Визуальные метки**: Иконки для операций с задачами (✅ создана, 📋 список, ✏️ обновлена)
- **Dropdown члена команды**: Показывает имя, роль и навыки из `team_members.json`
- **Форма создания задачи**: Material3 AlertDialog с валидацией полей

#### 🌿 6. Локальные Git операции

**Доступные команды:**
- `GitLocal покажи статус` - текущий статус репозитория
- `GitLocal список веток` - все локальные и удалённые ветки
- `GitLocal покажи diff` - изменения в файлах
- `GitLocal статус PR` - проверка статуса pull request

---

## Настройка и конфигурация

### Как настроить Perplexity API?

1. Зарегистрируйтесь на https://www.perplexity.ai/
2. Перейдите в раздел API keys
3. Создайте новый API key
4. Добавьте в `SecureData.kt`:
   ```kotlin
   val perplexityApiKey = "pplx-YOUR_KEY_HERE"
   ```

### Как настроить GitHub интеграцию?

**Шаг 1: Создайте Personal Access Token**

1. Откройте https://github.com/settings/tokens
2. "Generate new token" → "Generate new token (classic)"
3. Выберите scopes:
   - ✅ `repo` (полный доступ к репозиториям)
   - ✅ `read:packages`
   - ✅ `read:org`
4. Нажмите "Generate token"
5. **ВАЖНО**: Скопируйте токен сразу! Он больше не отобразится

**Шаг 2: Добавьте токен в приложение**

```kotlin
// SecureData.kt
val githubPersonalAccessToken = "ghp_ваш_токен_здесь"
```

**Шаг 3: Включите GitHub Assistant**

1. Откройте приложение
2. В чате нажмите иконку гаечного ключа (🔧)
3. Включите переключатель "GitHub Assistant"

### Как настроить Ollama?

**Установка:**

```bash
# macOS
brew install ollama

# Linux
curl https://ollama.ai/install.sh | sh

# Windows
# Скачайте с https://ollama.ai
```

**Загрузка модели для эмбеддингов:**

```bash
ollama pull nomic-embed-text
```

**Проверка:**

```bash
ollama list
# Должно показать: nomic-embed-text

curl http://localhost:11434/api/version
# Должно вернуть версию Ollama
```

**Использование GPU (ускорение):**

Ollama автоматически использует GPU если доступен:
- **NVIDIA GPU**: поддержка через CUDA
- **Apple Silicon**: поддержка через Metal
- **AMD GPU**: ограниченная поддержка

### Как настроить MCP серверы?

В приложении доступны 4 MCP сервера:

#### 1. RAG Server (Семантический поиск)

**URL**: `http://10.0.2.2:8080` (для эмулятора)
**Инструменты**: 6 (semantic_search, create_embedding, save_document, и др.)

**Настройка:**
- Автоматически подключается к локальному серверу
- Требует запущенный `http_mcp_server.py`

#### 2. GitHub Assistant

**URL**: `https://api.githubcopilot.com/mcp/`
**Инструменты**: 8 (get_repo, search_code, create_issue, и др.)

**Настройка:**
- Требует GitHub Personal Access Token
- Включается через MCP диалог

#### 3. Local Git

**URL**: Локальные git команды
**Инструменты**: 4 (git_status, git_branch, git_diff, git_pr_status)

**Настройка:**
- Работает с локальным репозиторием
- Путь настраивается в конфигурации

#### 4. Team Assistant (День 23-24)

**URL**: `http://10.0.2.2:8080` (тот же сервер что RAG)
**Инструменты**: 10 всего
- **CRM (4 tools)**: get_ticket, list_user_tickets, create_ticket, update_ticket
- **Tasks (6 tools)**: create_task, list_tasks, update_task, get_task, get_team_workload, search_similar_tasks

**Настройка:**
- Mock CRM данные в JSON файлах (tickets, users, tasks, team_members)
- Автоматически загружается при старте сервера
- Умное определение намерения (тикеты vs задачи vs гибрид)

### Как настроить пороги поиска?

**В приложении:**
1. Откройте экран чата
2. Найдите слайдер "Порог поиска"
3. Установите значение:
   - **0.3-0.5** - мягкий поиск (много результатов)
   - **0.6-0.7** - ⭐ рекомендуемое (баланс)
   - **0.8-0.95** - строгий поиск (только точные совпадения)

**В коде:**
```kotlin
// ChatViewModel.kt
private val _searchThreshold = MutableStateFlow(0.6f)
```

**В сервере:**
```python
# http_mcp_server.py
SEMANTIC_SEARCH_CONFIG = {
    'default_threshold': 0.6,
    'min_threshold': 0.3,
    'max_threshold': 0.95
}
```

---

## Использование

### Индексация документов

#### Через экран Ollama (текст)

1. Откройте приложение → экран "Ollama"
2. Выберите вкладку "Текст"
3. Вставьте или введите текст
4. Нажмите "Обработать текст"
5. Дождитесь "Документ сохранён"

**Производительность:**
- ~2 секунды на чанк (500-1000 символов)
- 15KB текста (~15 чанков) = 30-60 секунд

#### Через экран Ollama (PDF)

1. Откройте экран "Ollama"
2. Выберите вкладку "PDF"
3. Нажмите "Выбрать PDF"
4. Выберите файл
5. Дождитесь завершения обработки

**Ограничения:**
- Максимальный размер: 50 MB
- Рекомендуемый размер: до 10 MB
- Большие файлы могут занять несколько минут

#### Проверка индексации

**SQL запрос:**
```bash
sqlite3 server/data/embeddings.db "SELECT source_file, COUNT(*) FROM documents GROUP BY source_file;"
```

**Результат:**
```
authentication_faq.md|15
features_faq.md|12
troubleshooting.md|8
```

### Семантический поиск

#### Активация

Используйте ключевые слова в сообщении:
- "найди в документах"
- "поиск в базе"
- "что говорится в документах"
- "информация о"
- "расскажи о"
- "что такое"
- "как работает"
- "объясни"

**Пример:**
```
Вопрос: "Найди в документах как настроить 2FA"
→ AI автоматически использует semantic_search
→ Возвращает релевантные фрагменты с цитатами
```

#### Режимы поиска

**Обычный режим** (по умолчанию):
- Возвращает только результаты выше threshold
- Быстрый, точный

**Compare режим** (автоматически при пустых результатах):
- Возвращает ВСЕ результаты + отфильтрованные
- AI решает, использовать ли низкокачественные совпадения
- Помогает при строгом threshold

### GitHub операции

#### Информация о репозитории

```
GitWithLove покажи репозиторий vercel/next.js
```

**Ответ содержит:**
- Описание
- Количество звёзд и форков
- Основной язык программирования
- Лицензия
- Дата создания/обновления
- Открытые issues

#### Поиск кода

```
GitWithLove найди код "useEffect" в facebook/react
```

**Что ищет:**
- По всем файлам репозитория
- С использованием GitHub Code Search API
- Показывает файл, строку, контекст

#### Создание issue

```
GitWithLove создать issue в Golgoroth22/AI-with-Love:
Заголовок: Баг в авторизации
Описание: После обновления пароля не могу войти
```

**Результат:**
- Issue создан
- Возвращается номер и ссылка

### Code Review

#### Запуск ревью

```
ReviewPR сделай ревью для PR #45
```

**Что происходит:**
1. Получение метаданных PR (автор, название, статистика)
2. Получение списка изменённых файлов (до 30)
3. Анализ каждого файла на:
   - Security issues
   - Потенциальные баги
   - Проблемы производительности
   - Стилевые проблемы
4. Формирование отчёта с оценкой

**Ограничения:**
- Максимум 30 файлов в PR
- Анализ фокусируется на Kotlin и Python
- Для контекста нужны проиндексированные .md документы

#### Интеграция с документацией

Если вы проиндексировали файлы проекта (CLAUDE.md, README.md):
- AI будет ссылаться на стандарты проекта
- Рекомендации будут учитывать архитектуру
- Цитирование из документации в отчёте

### Support Assistant

#### Просмотр тикета

```
Support покажи тикет #1
```

**Карточка тикета показывает:**
- 🎫 ID и заголовок
- 👤 Информация о пользователе (имя, email)
- 📝 Описание проблемы
- 🔵 Статус (open/in_progress/resolved/closed)
- ⬆️ Приоритет (low/medium/high/critical)
- 🔐 Категория (authentication/features/troubleshooting/other)
- 📅 Дата создания и обновления

#### Помощь по тикету

```
Support тикет #2 - помоги решить проблему
```

**Workflow:**
1. Получение контекста тикета
2. Определение категории проблемы
3. Поиск решений в FAQ по категории
4. Формирование ответа:
   - Краткое описание проблемы из тикета
   - Возможные причины
   - Пошаговое решение из FAQ
   - Дополнительные рекомендации
   - Цитаты из документации

#### Создание тикета

```
Support создай тикет:
Пользователь: 2
Заголовок: Проблема с поиском документов
Описание: Семантический поиск не находит очевидные документы
Приоритет: high
Категория: features
```

**Результат:**
- Новый тикет создан с уникальным ID
- История инициализирована
- JSON файл обновлён

#### Обновление тикета

```
Support обнови тикет #1:
Статус: resolved
Заметка: Проблема решена после очистки кэша
```

**Что обновляется:**
- Статус тикета
- Время обновления (updated_at)
- История с timestamp и заметкой
- JSON файл сохраняется

### Team Assistant - Управление задачами

#### Создание задачи

**Способ 1: Через форму (быстрый)**

```
Создай задачу
```
или с предзаполненным названием:
```
Создай задачу - Исправить баг авторизации
```

**Процесс:**
1. Ключевое слово ("создай задачу") триггерит диалоговое окно
2. Форма открывается с полями:
   - **Название** - автоматически заполнено, если было указано после "-"
   - **Описание** - пустое поле для детального описания
   - **Приоритет** - выбор из dropdown (🔴 Высокий / 🟡 Средний / 🟢 Низкий)
3. Кнопка "Создать" активируется только когда все обязательные поля заполнены
4. При нажатии "Создать" задача создаётся через MCP напрямую (без AI)
5. Успешное сообщение появляется в чате

**Преимущества:**
- ⚡ Мгновенное создание (без AI loop)
- ✅ Валидация полей в реальном времени
- 📝 Структурированный ввод данных
- 🚀 Нет необходимости уточнять приоритет через диалог

**Способ 2: Через AI (комплексный)**

```
Мне нужна задача на исправление бага авторизации с высоким приоритетом
```

**Процесс:**
1. AI определяет намерение: TASK_MANAGEMENT
2. Проверяет похожие задачи (search_similar_tasks)
3. Ищет релевантную документацию (semantic_search)
4. Создаёт задачу (create_task)
5. Возвращает ID и рекомендации с контекстом

**Преимущества:**
- 🤖 AI анализирует контекст и предлагает решения
- 🔍 Автоматический поиск дубликатов
- 📚 Рекомендации из документации
- 👥 Умное предложение исполнителя

**Результат (оба способа):**
- Задача создана с уникальным ID
- Статус: todo (по умолчанию)
- Приоритет: согласно выбору
- Автор: текущий пользователь (из выбранного члена команды)
- Время создания: автоматически
- История инициализирована

#### Просмотр задач

```
Покажи задачи с приоритетом high
```

**Фильтры:**
- `status`: todo, in_progress, done, all
- `priority`: low, medium, high, all
- `assignee`: ID члена команды
- `limit`: количество результатов (по умолчанию 10)

**Ответ включает:**
- ID задачи
- Заголовок
- Статус
- Приоритет
- Исполнитель (assignee)

#### Назначение задачи

```
Назначь задачу #7 на developer_1
```

**Или умное назначение:**
```
Кто может взять задачу #7?
```

**AI анализирует:**
- Текущую загрузку команды (get_team_workload)
- Навыки членов команды (skills из team_members.json)
- Приоритет задачи
- Рекомендует оптимального исполнителя

#### Обновление статуса

```
Задача #7 статус in_progress
```

**Возможности:**
- Изменение статуса (todo → in_progress → done)
- Изменение приоритета
- Смена исполнителя
- Добавление заметок
- Полная история изменений

#### Проверка загрузки команды

```
Покажи загрузку команды
```

**Информация:**
- Список всех членов команды
- Роль (Backend/Frontend Developer)
- Навыки (Python, Kotlin, etc.)
- Текущая загрузка (количество активных задач)
- Доступность (available/busy)

**Фильтрация по роли:**

Поддерживается фильтрация на **русском и английском языках**. Сервер автоматически переводит русские запросы в английские роли:

```
# Русский язык (поддерживается)
Покажи список разработчиков
Кто из разработчиков свободен?
Покажи backend разработчиков
Кто такой босс?

# Английский язык (также работает)
Show me all developers
Who is available from backend team?
```

**Поддерживаемые переводы:**
- **разработчик/разработчиков** → developer (находит Backend и Frontend Developer)
- **backend** → backend (находит Backend Developer)
- **frontend** → frontend (находит Frontend Developer)
- **босс/начальник** → boss
- **менеджер** → manager
- **тестировщик** → tester
- **qa** → qa
- **дизайнер** → designer
- **devops** → devops

**Примеры запросов:**
```
# Все разработчики
"Покажи список разработчиков"
→ Возвращает: Антон Многодумов (Backend) + Наташа Петрова (Frontend)

# Только Backend
"Кто из Backend разработчиков свободен?"
→ Возвращает: Антон Многодумов

# Только Frontend
"Покажи Frontend разработчиков"
→ Возвращает: Наташа Петрова
```

#### Создание задачи из тикета

```
Создай задачу из тикета #12
```

**Workflow:**
1. Получает контекст тикета (get_ticket)
2. Извлекает заголовок и описание
3. Проверяет похожие задачи
4. Создаёт задачу с `related_ticket_id=12`
5. Обновляет тикет с ссылкой на задачу
6. Возвращает обе связанные сущности

**Преимущества:**
- Полная трассируемость (тикет ↔ задача)
- История в обоих объектах
- Контекст для разработчика

### Локальный Git

#### Статус репозитория

```
GitLocal покажи статус репозитория
```

**Информация:**
- Текущая ветка
- Изменённые файлы (modified, staged, untracked)
- Количество коммитов впереди/позади remote

#### Список веток

```
GitLocal какие есть ветки?
```

**Показывает:**
- Все локальные ветки
- Удалённые ветки
- Текущая активная ветка отмечена *

#### Diff файлов

```
GitLocal покажи изменения в файлах
```

**Типы diff:**
- Unstaged changes (не добавленные в staging)
- Staged changes (добавленные через git add)

---

## Архитектура

### Общая структура

```
┌─────────────────────────────────────────────────────┐
│             Android Application                      │
│  (Kotlin + Jetpack Compose + Coroutines)           │
└───────────────────┬─────────────────────────────────┘
                    │
                    ├─→ Perplexity Agentic API
                    │   (AI ответы + tool calling)
                    │
                    ├─→ MCP Client Manager
                    │   (Маршрутизация между серверами)
                    │   │
                    │   ├─→ RAG Server (local)
                    │   │   - semantic_search
                    │   │   - process_text_chunks
                    │   │   - create_embedding
                    │   │
                    │   ├─→ GitHub MCP Server (remote)
                    │   │   - get_repo
                    │   │   - search_code
                    │   │   - get_pull_request
                    │   │
                    │   ├─→ Local Git (local commands)
                    │   │   - git_status
                    │   │   - git_branch
                    │   │
                    │   └─→ Team Assistant (local)
                    │       - CRM: get_ticket, create_ticket, update_ticket
                    │       - Tasks: create_task, list_tasks, update_task,
                    │                get_task, get_team_workload,
                    │                search_similar_tasks
                    │
                    └─→ Ollama (local)
                        - Генерация эмбеддингов
                        - nomic-embed-text модель
```

### Три-слойная архитектура приложения

#### 1. Presentation Layer (UI)

**Технологии**: Jetpack Compose, Material 3

**Компоненты:**
- `ChatScreen.kt` - главный чат с AI
- `OllamaScreen.kt` - экран индексации документов
- `TeamScreen.kt` - экран ассистента команды (тикеты + задачи)
- `LaunchScreen.kt` - навигационный hub

**Обязанности:**
- Отображение сообщений
- Ввод пользователя
- MCP tool результаты (карточки)
- Управление MCP серверами
- Диалоговые формы (создание задач)

#### 2. Domain Layer (Business Logic)

**Технологии**: Kotlin Coroutines, StateFlow

**Компоненты:**
- `ChatViewModel.kt` - агентивная оркестрация
- `OllamaViewModel.kt` - обработка документов
- `SupportViewModel.kt` - управление задачами и тикетами
- `ChatRepository.kt` - операции с БД

**Обязанности:**
- Agentic loop (итеративное выполнение инструментов)
- Keyword detection (определение намерений и триггеров)
- Tool execution (вызов MCP инструментов)
- Dialog compression (сжатие истории)
- State management (StateFlow для UI состояний)
- Form dialog state (для создания задач)

#### 3. Data Layer (Хранение и сеть)

**Технологии**: Room, Ktor, SQLite

**Компоненты:**
- `PerplexityApiService` - HTTP клиент для AI API
- `McpClientManager` - мультисерверная маршрутизация
- `McpClient` - JSON-RPC 2.0 протокол
- `AppDatabase` - Room БД для сообщений
- `EmbeddingsDatabase` - SQLite для эмбеддингов

**Обязанности:**
- HTTP запросы
- Кэширование
- Персистентность данных
- MCP коммуникация

### Agentic Loop

**Принцип работы:**

```kotlin
var iterations = 0
val maxIterations = 5

while (hasToolCalls(response) && iterations < maxIterations) {
    iterations++

    // 1. Извлечь tool calls из ответа AI
    response.output?.filter { it.type == "function_call" }?.forEach { toolCall ->

        // 2. Выполнить tool
        val result = executeAgenticToolCall(toolCall.name, toolCall.arguments)

        // 3. Сохранить результат
        toolResults.add("Tool ${toolCall.name} result: ${result.result}")
    }

    // 4. Отправить результаты обратно в AI
    currentInput = "$currentInput\n\nTool results:\n${toolResults.joinToString("\n")}"
    response = perplexityService.sendAgenticRequest(...)
}
```

**Почему это важно:**
- AI может делать последовательные вызовы инструментов
- Результаты одного tool используются для следующего
- Максимум 5 итераций для предотвращения зацикливания

**Пример workflow:**

```
Пользователь: "ReviewPR сделай ревью для PR #45"

Итерация 1:
  AI вызывает: get_pull_request(pr_number=45)
  Результат: метаданные PR

Итерация 2:
  AI вызывает: get_pr_files(pr_number=45)
  Результат: список изменённых файлов

Итерация 3:
  AI анализирует файлы и формирует отчёт
  Финальный ответ пользователю
```

### MCP (Model Context Protocol)

**Что это:**
Протокол для подключения внешних инструментов к AI-системам.

**Структура:**

```
┌─────────────────────┐
│   AI (Perplexity)   │
│                     │
│  Решает какие tools │
│  вызывать и как     │
└──────────┬──────────┘
           │
           │ function_call
           ▼
┌─────────────────────┐
│  MCP Client Manager │
│                     │
│  Маршрутизация по   │
│  названию tool      │
└──────────┬──────────┘
           │
           ├─→ RAG Server
           ├─→ GitHub Server
           ├─→ Local Git
           └─→ Support Server
```

**JSON-RPC 2.0 формат:**

```json
// Request
{
  "jsonrpc": "2.0",
  "id": 1,
  "method": "tools/call",
  "params": {
    "name": "semantic_search",
    "arguments": {
      "query": "как настроить 2FA",
      "limit": 5,
      "threshold": 0.6
    }
  }
}

// Response
{
  "jsonrpc": "2.0",
  "id": 1,
  "result": {
    "content": [{
      "type": "text",
      "text": "{\"results\": [...]}"
    }]
  }
}
```

### Локальная обработка документов

**Преимущества (относительно серверной):**
- ⚡ **7x быстрее** (45s vs 320s для 15KB)
- ✅ Нет таймаутов
- 🔒 Приватность (данные не покидают устройство)
- 💰 Экономия ресурсов сервера

**Процесс:**

```
Документ (текст/PDF)
    ↓
1. Извлечение текста (PDFBox для PDF)
    ↓
2. Чанкирование (1000 символов, 200 overlap)
    ↓
3. Локальные эмбеддинги (Ollama: nomic-embed-text)
    ↓
4. Отправка на сервер (только эмбеддинги + текст)
    ↓
5. Сохранение в SQLite
```

**Параллельная обработка:**

```python
# ThreadPoolExecutor для ускорения
with concurrent.futures.ThreadPoolExecutor(max_workers=4) as executor:
    futures = [executor.submit(process_chunk, chunk) for chunk in chunks]
    results = [future.result() for future in futures]
```

**Производительность:**
- 1 чанк = ~2 секунды
- 15 чанков = ~30-40 секунд (параллельно)
- 100 чанков = ~3-4 минуты

### Semantic Search (Семантический поиск)

**Алгоритм:**

1. **Генерация эмбеддинга запроса**
   ```python
   query_embedding = generate_embedding(query)  # 768-мерный вектор
   ```

2. **Вычисление косинусного сходства**
   ```python
   for doc in documents:
       similarity = cosine_similarity(query_embedding, doc.embedding)
   ```

3. **Фильтрация по порогу**
   ```python
   results = [doc for doc in documents if doc.similarity >= threshold]
   ```

4. **Сортировка по релевантности**
   ```python
   results.sort(key=lambda x: x.similarity, reverse=True)
   ```

**Формула косинусного сходства:**

```
similarity = (A · B) / (||A|| * ||B||)

где:
  A · B = скалярное произведение векторов
  ||A|| = норма вектора A
  ||B|| = норма вектора B

Результат: от -1 до 1 (обычно 0 до 1 для текстов)
```

**Оптимизация:**
- Предварительное вычисление норм векторов
- Индексы в SQLite для быстрого поиска
- Batch обработка для нескольких запросов

### Dialog Compression (Сжатие диалога)

**Зачем:**
- Ограничение контекста AI (обычно 32k-128k токенов)
- Сохранение истории без переполнения
- Уменьшение стоимости API запросов

**Когда срабатывает:**
- После каждых 5 сообщений пользователя
- Минимум 5 сообщений с момента запуска приложения

**Процесс:**

```kotlin
// 1. Сбор сообщений для сжатия
val messagesToCompress = messagesAfterSummary

// 2. Формирование prompt
val summaryPrompt = """
Создай краткое резюме следующего диалога,
сохранив ключевую информацию...

${messagesToCompress.joinToString("\n")}
"""

// 3. Запрос к AI
val summary = perplexityService.generateSummary(summaryPrompt)

// 4. Сохранение резюме
chatRepository.insertSummary(summary)

// 5. Пометка сжатых сообщений
messagesToCompress.forEach { it.isCompressed = true }
```

**Структура резюме:**
```
📝 Резюме предыдущих сообщений:
- Пользователь спрашивал о настройке 2FA
- Был предоставлен пошаговый гайд
- Обсуждались проблемы с GitHub токеном
- Решение: пересоздать токен с правильными scopes
```

### Database Schema (Room)

**Таблицы:**

#### 1. user_messages

```sql
CREATE TABLE user_messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    message TEXT NOT NULL,
    timestamp INTEGER NOT NULL,
    is_compressed INTEGER DEFAULT 0
);
```

#### 2. assistant_messages

```sql
CREATE TABLE assistant_messages (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    message TEXT NOT NULL,
    timestamp INTEGER NOT NULL,
    prompt_tokens INTEGER DEFAULT 0,
    completion_tokens INTEGER DEFAULT 0,
    mcp_tool_info_json TEXT,
    is_compressed INTEGER DEFAULT 0
);
```

**Поле `mcp_tool_info_json`:**
Хранит метаданные о вызванных MCP tools в JSON формате:

```json
{
  "toolName": "get_ticket",
  "emoji": "🎫",
  "description": "Получение тикета #1",
  "responseBody": "{\"success\":true,\"ticket\":{...}}"
}
```

#### 3. chat_summaries

```sql
CREATE TABLE chat_summaries (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    summary TEXT NOT NULL,
    timestamp INTEGER NOT NULL,
    messages_count INTEGER NOT NULL
);
```

---

## Устранение неполадок

### MCP Server не запускается

**Симптом**: Ошибка при запуске `python3 http_mcp_server.py`

**Возможные причины:**

1. **Порт 8080 занят**
   ```bash
   # Проверка
   lsof -i :8080

   # Решение
   kill -9 <PID>
   ```

2. **Отсутствуют зависимости**
   ```bash
   pip3 install aiohttp aiosqlite
   ```

3. **Неверная версия Python**
   ```bash
   python3 --version
   # Требуется Python 3.9+
   ```

4. **Проблемы с правами доступа**
   ```bash
   chmod +x server/http_mcp_server.py
   ```

### Ollama не работает

**Симптом**: Ошибка "Connection refused" при обработке документов

**Решения:**

1. **Проверьте запущен ли Ollama**
   ```bash
   curl http://localhost:11434/api/version
   ```

   Если ошибка:
   ```bash
   ollama serve
   ```

2. **Проверьте загружена ли модель**
   ```bash
   ollama list
   ```

   Если нет `nomic-embed-text`:
   ```bash
   ollama pull nomic-embed-text
   ```

3. **Проверьте порт**
   ```bash
   # Ollama должен слушать на 11434
   lsof -i :11434
   ```

4. **Переустановите Ollama** (крайний случай)
   ```bash
   # macOS
   brew uninstall ollama
   brew install ollama
   ```

### Семантический поиск не находит документы

**Симптом**: Запрос "найди в документах X" возвращает пустой результат

**Диагностика:**

1. **Проверьте индексацию**
   ```bash
   sqlite3 server/data/embeddings.db "SELECT COUNT(*) FROM documents;"
   ```

   Если 0 → документы не проиндексированы

2. **Проверьте threshold**
   - Слишком высокий порог (>0.8) может исключать все результаты
   - Попробуйте снизить до 0.5

3. **Проверьте RAG Server включён**
   - В чате → иконка 🔧 → "RAG Server" должен быть включен

4. **Проверьте логи сервера**
   ```bash
   tail -f /tmp/mcp_server.log
   ```

   Ищите ошибки при semantic_search

**Решения:**

1. **Переиндексируйте документы**
   - Ollama screen → вставьте текст заново

2. **Понизьте threshold**
   - В чате → слайдер "Порог поиска" → 0.5

3. **Используйте более конкретные запросы**
   ```
   ❌ "найди информацию"
   ✅ "найди в документах как настроить 2FA"
   ```

### GitHub операции не работают

**Симптом**: Ошибка "Unauthorized" или "Not found"

**Проверки:**

1. **Токен установлен?**
   ```kotlin
   // SecureData.kt
   val githubPersonalAccessToken = "ghp_..." // не пустая строка
   ```

2. **Токен валиден?**
   ```bash
   curl -H "Authorization: token ghp_YOUR_TOKEN" https://api.github.com/user
   ```

   Должно вернуть информацию о вашем аккаунте

3. **Правильные scopes?**
   - Откройте https://github.com/settings/tokens
   - Проверьте что выбраны: `repo`, `read:packages`, `read:org`

4. **GitHub Assistant включён?**
   - Чат → 🔧 → "GitHub Assistant" = ON

**Решения:**

1. **Пересоздайте токен**
   - Удалите старый токен на GitHub
   - Создайте новый с правильными scopes
   - Обновите `SecureData.kt`

2. **Проверьте репозиторий публичный**
   - Для приватных репозиториев нужен scope `repo` (полный)

### App не собирается

**Симптом**: Build failed в Android Studio

**Частые причины:**

1. **Lint ошибки**
   ```bash
   # Пропустить lint при сборке
   ./gradlew assembleDebug -x lintVitalAnalyzeRelease
   ```

2. **Отсутствует SecureData.kt**
   - Создайте файл согласно шаблону выше

3. **Версия Kotlin несовместима**
   ```gradle
   // build.gradle.kts
   kotlin("jvm") version "1.9.20" // проверьте версию
   ```

4. **KSP ошибки**
   ```bash
   ./gradlew clean
   ./gradlew build
   ```

### Медленная обработка документов

**Симптом**: Индексация 10KB текста занимает >5 минут

**Причины и решения:**

1. **Ollama использует CPU вместо GPU**
   ```bash
   # Проверка
   ollama run nomic-embed-text "test"
   # Должно быть быстро (<1 сек)
   ```

   **Решение для macOS (Apple Silicon):**
   - Ollama автоматически использует Metal
   - Обновите до последней версии

   **Решение для NVIDIA GPU:**
   ```bash
   # Проверьте CUDA установлена
   nvidia-smi
   ```

2. **Большие чанки**
   ```python
   # server/http_mcp_server.py
   chunk_size = 500  # уменьшите с 1000
   ```

3. **Медленная сеть (при отправке на сервер)**
   - Обработка локальная, но отправка может тормозить
   - Проверьте: `ping 10.0.2.2` (для эмулятора)

### Code Review выдаёт неточности

**Симптом**: ReviewPR находит несуществующие проблемы

**Причины:**

1. **Нет контекста проекта**
   - AI не знает ваши соглашения
   - Решение: проиндексируйте CLAUDE.md, README.md

2. **Слишком большой PR**
   - >30 файлов → AI может пропустить контекст
   - Решение: разделите на несколько PR

3. **Специфичный код**
   - AI лучше понимает общие паттерны
   - Для domain-specific кода добавьте комментарии

**Улучшение качества:**

1. **Добавьте документацию проекта**
   ```
   Ollama screen → вставьте:
   - CLAUDE.md (архитектурные решения)
   - README.md (общий обзор)
   - CONTRIBUTING.md (соглашения)
   ```

2. **Используйте четкие commit messages**
   - AI анализирует не только код, но и описание изменений

3. **Добавьте комментарии к сложному коду**

### Team Assistant (День 24) - Проблемы

**Симптом**: Ошибка компиляции "Argument type mismatch" в tool builders

**Причина**: JsonArray требует JsonPrimitive для строковых элементов

**Решение:**
```kotlin
// ❌ НЕПРАВИЛЬНО:
putJsonArray("enum") {
    add("low")
    add("medium")
}

// ✅ ПРАВИЛЬНО:
putJsonArray("enum") {
    add(JsonPrimitive("low"))
    add(JsonPrimitive("medium"))
}
```

**Локации:**
- `SupportViewModel.kt` - все tool builder методы
- Везде где используется `putJsonArray` со строками

---

**Симптом**: Ошибка "Call requires API level 26" при использовании java.time

**Причина**: java.time.LocalDate требует API 26, проект на минимальном API 25

**Решение:**
```kotlin
// ❌ НЕПРАВИЛЬНО (API 26):
val currentDate = java.time.LocalDate.now()

// ✅ ПРАВИЛЬНО (API 25):
val currentDate = SimpleDateFormat("yyyy-MM-dd", Locale.getDefault())
    .format(Date())
```

**Локация:** `SupportViewModel.kt` в методе `buildSupportInstructions()`

---

**Симптом**: Форма создания задачи не появляется при вводе "создай задачу"

**Причина**: Ключевое слово написано с ошибкой или используется неподдерживаемая фраза

**Решение:**

Убедитесь что используете точное написание одного из ключевых слов (регистр не важен):
- ✅ "создай задачу" → форма появится
- ✅ "новая задача" → форма появится
- ✅ "добавь задачу" → форма появится
- ✅ "create task" → форма появится
- ✅ "new task" → форма появится
- ✅ "add task" → форма появится

**Частые ошибки:**
- ❌ "создай зачачу" → опечатка, форма НЕ появится
- ❌ "сделай задачу" → не поддерживается
- ❌ "создать задачу" → не поддерживается (но можно добавить)

**Отладка:**
1. Проверьте логи (Tag: SupportViewModel)
   - Должно быть: `🎯 Task creation dialog triggered`
2. Если сообщение ушло в AI вместо формы → проверьте правописание
3. Если форма не открывается даже с правильным ключевым словом:
   - Проверьте что `_showCreateTaskDialog.value = true` в `sendMessage()`
   - Убедитесь что Composable наблюдает за `showCreateTaskDialog.collectAsState()`

**Добавление новых ключевых слов:**
```kotlin
// SupportViewModel.kt
private fun detectTaskCreationKeywords(message: String): Boolean {
    val lowerMessage = message.lowercase()
    val taskCreationKeywords = listOf(
        "создай задачу",
        "новая задача",
        "добавь задачу",
        "create task",
        "new task",
        "add task",
        "сделай задачу" // добавьте здесь
    )
    return taskCreationKeywords.any { lowerMessage.contains(it) }
}
```

---

**Симптом**: Unit тесты падают с "AssertionError" или таймаутами

**Причина**: ViewModelScope coroutines на Dispatchers.IO не синхронизированы с test dispatcher

**Решение:**
```kotlin
// В test setup:
private val testDispatcher = UnconfinedTestDispatcher()

private fun createViewModel() {
    viewModel = ChatViewModel(...)
    Thread.sleep(600) // Ждём init IO coroutines
}

// В тестах после async операций:
viewModel.sendMessage("Test")
Thread.sleep(300) // Ждём завершения async sendMessage
```

**Рекомендуемые задержки:**
- createViewModel(): 600ms
- после sendMessage(): 300-500ms
- после agentic loop: 500ms

---

**Симптом**: Задачи не отображаются в TopAppBar

**Причина**: TaskContext не обновляется после операций

**Проверки:**
1. `refreshTaskContext()` вызывается после create/update
2. `_taskContext.value` устанавливается корректно
3. `taskContext` StateFlow собирается в Composable
4. TopAppBar наблюдает за `taskContext.collectAsState()`

**Решение:**
```kotlin
// В SupportViewModel после task операций:
private suspend fun refreshTaskContext() {
    val result = mcpClientManager.callTool(
        toolName = "list_tasks",
        arguments = mapOf("status" to "all", "limit" to 100),
        enabledServers = listOf("support")
    )

    // Парсинг и обновление
    _taskContext.value = TaskContext(activeCount, highPriorityCount)
}
```

---

**Симптом**: "No such tool: create_task" при вызове

**Причина**: MCP сервер не перезапущен после добавления новых tools

**Решение:**
```bash
# Остановить старый сервер
pkill -f http_mcp_server.py

# Запустить новый
cd server
python3 http_mcp_server.py

# Проверить количество tools (должно быть 28)
curl -X POST http://localhost:8080 \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","method":"tools/list","id":1}' \
  | jq '.result.tools | length'
```

---

**Симптом**: "Покажи список разработчиков" возвращает пустой список (0 members)

**Причина**: ~~Языковое несоответствие между русским запросом и английскими ролями в базе~~ **ИСПРАВЛЕНО** в текущей версии

**Решение (автоматически применяется):**

Начиная с версии Day 24 (2026-02-16), сервер автоматически переводит русские роли в английские:

```python
# В http_mcp_server.py (tool_get_team_workload)
role_translations = {
    'разработчик': 'developer',
    'разработчиков': 'developer',
    'backend': 'backend',
    'frontend': 'frontend',
    'босс': 'boss',
    # ... и другие
}
```

**Проверка работы:**

```bash
# Тест с русским фильтром
curl -X POST http://localhost:8080 \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "tools/call",
    "params": {
      "name": "get_team_workload",
      "arguments": {"role_filter": "разработчик"}
    },
    "id": 1
  }'

# Ожидаемый результат: 2 members (Антон и Наташа)
```

**Если проблема сохраняется:**

1. Перезапустите MCP сервер (применение изменений):
   ```bash
   pkill -f http_mcp_server.py
   cd server && python3 http_mcp_server.py
   ```

2. Проверьте версию файла `http_mcp_server.py`:
   ```bash
   grep -A 5 "role_translations" server/http_mcp_server.py
   # Должен показать словарь переводов
   ```

3. Убедитесь что team_members.json содержит роли на английском:
   ```bash
   cat server/data/team_members.json | grep "role"
   # Должно быть: "Backend Developer", "Frontend Developer"
   ```

---

## Разработка

### Как добавить новый MCP инструмент?

#### Шаг 1: Серверная часть (Python)

**Файл**: `server/http_mcp_server.py`

```python
# 1. Добавьте tool definition в handle_tools_list()
{
    'name': 'my_new_tool',
    'description': 'Описание того, что делает инструмент',
    'inputSchema': {
        'type': 'object',
        'properties': {
            'param1': {'type': 'string', 'description': '...'},
            'param2': {'type': 'integer', 'description': '...'}
        },
        'required': ['param1']
    }
}

# 2. Добавьте dispatcher в handle_tools_call()
elif tool_name == 'my_new_tool':
    result = self.tool_my_new_tool(arguments)

# 3. Реализуйте handler
def tool_my_new_tool(self, args):
    """Обработка my_new_tool"""
    param1 = args.get('param1')
    param2 = args.get('param2', default_value)

    # Ваша логика
    result = do_something(param1, param2)

    return {
        'success': True,
        'result': result
    }
```

#### Шаг 2: Клиентская часть (Kotlin)

**Файл**: `app/.../ChatViewModel.kt`

```kotlin
// 1. Добавьте tool builder
private fun buildMyNewTool(): AgenticTool {
    return AgenticTool(
        type = "function",
        name = "my_new_tool",
        description = "Описание для AI когда использовать",
        parameters = buildJsonObject {
            put("type", "object")
            putJsonObject("properties") {
                putJsonObject("param1") {
                    put("type", "string")
                    put("description", "Описание параметра")
                }
            }
            putJsonArray("required") {
                add("param1")
            }
        }
    )
}

// 2. Добавьте keyword detection (опционально)
private fun userMentionsMyNewFeature(message: String): Boolean {
    val lowerMessage = message.lowercase()
    return lowerMessage.contains("keyword1") ||
           lowerMessage.contains("keyword2")
}

// 3. Добавьте tool execution
"my_new_tool" -> {
    runAndCatch {
        val args = parseToolArguments(arguments)
        logD("🎯 Calling my_new_tool")

        val mcpResult = mcpClientManager.callTool(
            toolName = "my_new_tool",
            arguments = args,
            enabledServers = enabledServers
        )

        ToolExecutionResult(
            result = parseResultFromMcp(mcpResult),
            mcpToolInfo = McpToolInfo(
                toolName = "my_new_tool",
                emoji = "🎯",
                description = "Результат my_new_tool",
                responseBody = mcpResult
            )
        )
    }.getOrElse { error ->
        logE("❌ my_new_tool failed", error)
        ToolExecutionResult(result = error.message ?: "Error", mcpToolInfo = null)
    }
}

// 4. Добавьте в sendWithAgenticApi()
val useMyNewFeature = userMentionsMyNewFeature(userMessage) && isMyFeatureEnabled()
if (useMyNewFeature) {
    tools.add(buildMyNewTool())
}
```

#### Шаг 3: UI компонент (опционально)

**Файл**: `app/.../ChatScreen.kt`

```kotlin
@Composable
private fun MyNewToolResultCard(result: MyNewToolResult) {
    Card(
        modifier = Modifier.fillMaxWidth(),
        colors = CardDefaults.cardColors(
            containerColor = MaterialTheme.colorScheme.surfaceVariant
        )
    ) {
        Column(modifier = Modifier.padding(12.dp)) {
            Text("🎯 My New Tool", style = MaterialTheme.typography.titleMedium)
            // Ваша UI логика
        }
    }
}

// Интеграция в message rendering
when (toolInfo.toolName) {
    "my_new_tool" -> {
        parseMyNewToolResult(toolInfo.responseBody)?.let {
            MyNewToolResultCard(result = it)
        }
    }
}
```

### Структура проекта

```
AI-with-Love/
├── День 24. Ассистент команды/
│   ├── app/                           # Android приложение
│   │   ├── src/main/java/.../
│   │   │   ├── ui/screen/            # Compose UI
│   │   │   │   ├── ChatScreen.kt
│   │   │   │   ├── OllamaScreen.kt
│   │   │   │   ├── TeamScreen.kt         # 🔄 Renamed from SupportScreen
│   │   │   │   └── LaunchScreen.kt
│   │   │   ├── viewmodel/            # Business logic
│   │   │   │   ├── ChatViewModel.kt
│   │   │   │   └── OllamaViewModel.kt
│   │   │   ├── data/                 # Data layer
│   │   │   │   ├── PerplexityApiServiceImpl.kt
│   │   │   │   └── ChatRepository.kt
│   │   │   ├── mcp/                  # MCP protocol
│   │   │   │   ├── McpClient.kt
│   │   │   │   ├── McpClientManager.kt
│   │   │   │   └── McpServerConfig.kt
│   │   │   ├── database/             # Room database
│   │   │   │   └── AppDatabase.kt
│   │   │   └── util/                 # Utilities
│   │   │       └── SecureData.kt
│   │   └── build.gradle.kts
│   │
│   ├── server/                        # MCP Server (Python)
│   │   ├── http_mcp_server.py        # Главный сервер
│   │   ├── data/
│   │   │   ├── embeddings.db         # SQLite БД эмбеддингов
│   │   │   ├── crm_users.json        # Mock CRM пользователи (для тикетов)
│   │   │   ├── crm_tickets.json      # Mock CRM тикеты
│   │   │   ├── tasks.json            # 🆕 Task storage (Day 24)
│   │   │   ├── team_members.json     # 🆕 Team data (Day 24) - используется для dropdown
│   │   │   └── support_faq/          # FAQ документы
│   │   │       ├── authentication_faq.md
│   │   │       ├── features_faq.md
│   │   │       └── troubleshooting_faq.md
│   │   └── test_http_mcp_server.py   # Тесты
│   │
│   ├── README.md                      # Описание проекта
│   ├── FAQ.md                         # Этот файл
│   ├── CLAUDE.md                      # Гайд для разработчиков
│   ├── TESTING_SUMMARY.md             # Результаты тестирования
│   └── LOCAL_PROCESSING.md            # Документация локальной обработки
```

### Тестирование

#### Юнит-тесты (Android)

```bash
# Запуск всех тестов
./gradlew test

# Конкретный тест
./gradlew test --tests ChatViewModelProductionTest

# С подробным выводом
./gradlew test --info
```

#### Тесты сервера (Python)

```bash
cd server
python3 test_http_mcp_server.py
```

**Ожидаемый результат:**
```
Ran 26 tests in 2.341s
OK
```

#### Интеграционное тестирование

1. **Запустите сервер**
   ```bash
   cd server
   python3 http_mcp_server.py
   ```

2. **Тестируйте через curl**
   ```bash
   # Проверка health
   curl http://localhost:8080/health

   # Список инструментов
   curl -X POST http://localhost:8080 \
     -H "Content-Type: application/json" \
     -d '{"jsonrpc":"2.0","method":"tools/list","id":1}'

   # Вызов semantic_search
   curl -X POST http://localhost:8080 \
     -H "Content-Type: application/json" \
     -d '{
       "jsonrpc": "2.0",
       "method": "tools/call",
       "params": {
         "name": "semantic_search",
         "arguments": {"query": "test", "limit": 5}
       },
       "id": 1
     }'
   ```

### CI/CD

**GitHub Actions** (пример):

```yaml
name: Build and Test

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'

      - name: Build Android
        run: ./gradlew assembleDebug

      - name: Run tests
        run: ./gradlew test

      - name: Test MCP Server
        run: |
          cd server
          python3 -m pip install -r requirements.txt
          python3 test_http_mcp_server.py
```

### Лицензия и вклад

**Лицензия**: MIT (или укажите вашу)

**Как внести вклад:**
1. Fork репозитория
2. Создайте feature branch
3. Commit изменения
4. Push в ваш fork
5. Создайте Pull Request

**Стандарты кода:**
- Kotlin: следуйте [Android Kotlin Style Guide](https://developer.android.com/kotlin/style-guide)
- Python: следуйте PEP 8
- Commit messages: используйте [Conventional Commits](https://www.conventionalcommits.org/)

---

## Дополнительные ресурсы

### Документация

- [README.md](README.md) - Основное описание проекта
- [CLAUDE.md](CLAUDE.md) - Детальный гайд для разработчиков
- [TESTING_SUMMARY.md](TESTING_SUMMARY.md) - Результаты тестирования
- [LOCAL_PROCESSING.md](LOCAL_PROCESSING.md) - Локальная обработка документов

### Внешние ссылки

- [Perplexity AI](https://www.perplexity.ai/) - AI API
- [Ollama](https://ollama.ai/) - Локальные LLM и эмбеддинги
- [Model Context Protocol](https://github.com/modelcontextprotocol) - MCP спецификация
- [Jetpack Compose](https://developer.android.com/jetpack/compose) - Android UI
- [GitHub API](https://docs.github.com/en/rest) - GitHub REST API

### Сообщество

- **Issues**: https://github.com/Golgoroth22/AI-with-Love/issues
- **Discussions**: GitHub Discussions (если включены)
- **Email**: (укажите ваш email для связи)

---

## Заключение

**AI with Love** - это комплексный пример современного AI-приложения, демонстрирующий:

✅ Агентивные AI паттерны (agentic loops)
✅ RAG с семантическим поиском
✅ MCP интеграции с множественными серверами
✅ Локальная обработка данных (приватность)
✅ Автоматизация developer workflows
✅ Унифицированный Team Assistant (тикеты + задачи + умное назначение)

**Следующие шаги:**
1. Настройте окружение по [Быстрому старту](#быстрый-старт)
2. Проиндексируйте FAQ документы
3. Попробуйте различные функции
4. Изучите код для понимания архитектуры
5. Добавьте свои собственные MCP инструменты!

**Обратная связь приветствуется!** 🚀

---

*Последнее обновление: День 24 (2026-02-16) - добавлена поддержка русских запросов для фильтрации команды*
*Версия FAQ: 2.2*
