# День 24. Ассистент команды

1. Объедините всё: ассистент, который знает проект (RAG), умеет взаимодействовать с сервисами команды (MCP) и может:
- создавать задачи (через API)
- отвечать на вопросы о текущем статусе проекта
- давать рекомендации по приоритетам

Пример: “Покажи задачи с приоритетом high и предложи, что делать первым”

- `Результат`: интегрированный командный ассистент
- `Формат`: Видео + Код

[Видео результата](https://drive.google.com/file/d/16cdibHCXp4dgekd7YrAGYWN5glXN8cQs/view?usp=sharing)

Код mcp-сервера в директории **server/**


## 🔍 RAG & Document Processing Tools (6 инструментов)

| Инструмент | Описание | Сервер |
|------------|----------|--------|
| `semantic_search` | Поиск релевантных фрагментов документов с использованием семантического сходства | RAG Server |
| `create_embedding` | Генерация эмбеддингов для текста с использованием модели Ollama nomic-embed-text | RAG Server |
| `save_document` | Сохранение документа с эмбеддингами и информацией о цитировании в локальную БД | RAG Server |
| `search_similar` | Поиск похожих документов с использованием косинусного сходства в локальной БД | RAG Server |
| `process_pdf` | Извлечение текста из PDF, разбиение на фрагменты и сохранение с эмбеддингами | RAG Server |
| `process_text_chunks` | Обработка извлечённого текста на фрагменты с эмбеддингами (параллельная обработка) | RAG Server |

## 🐙 GitHub MCP Tools (8 инструментов)

| Инструмент | Описание | Активация |
|------------|----------|-----------|
| `get_repo` | Получение детальной информации о GitHub репозитории (звёзды, форки, описание и т.д.) | `"GitWithLove"` |
| `search_code` | Поиск кода в GitHub репозиториях с использованием синтаксиса поиска GitHub | `"GitWithLove"` |
| `create_issue` | Создание нового issue в GitHub репозитории | `"GitWithLove"` |
| `list_issues` | Список issues из репозитория (фильтр по состоянию: open/closed/all) | `"GitWithLove"` |
| `list_commits` | Список коммитов из истории репозитория | `"GitWithLove"` |
| `get_repo_content` | Получение содержимого файлов или списка директорий из GitHub репозитория | `"GitWithLove"` |
| `get_pull_request` 🆕 | Получение метаданных Pull Request (заголовок, автор, статистика, статус) | `"ReviewPR"` |
| `get_pr_files` 🆕 | Получение списка изменённых файлов PR с diff и patch (до 30 файлов) | `"ReviewPR"` |

## 🌿 Local Git Tools (4 инструмента)

| Инструмент | Описание | Активация |
|------------|----------|-----------|
| `git_status` | Получение текущего статуса репозитория (изменённые файлы, staged файлы, текущая ветка) | `"GitLocal"` |
| `git_branch` | Список всех локальных и удалённых веток с указанием текущей активной ветки | `"GitLocal"` |
| `git_diff` | Получение diff для файлов (unstaged или staged изменения) | `"GitLocal"` |
| `git_pr_status` | Проверка статуса Pull Request для текущей ветки, комбинируя локальный git с GitHub API | `"GitLocal"` |

## 🎫 Support/CRM Tools (4 инструмента)

| Инструмент | Описание | Сервер |
|------------|----------|--------|
| `get_ticket` | Получение деталей тикета по ID (статус, приоритет, описание, полная история) | Support Server |
| `list_user_tickets` | Список всех тикетов пользователя с опциональной фильтрацией по статусу | Support Server |
| `create_ticket` | Создание нового тикета поддержки с автоматической генерацией ID | Support Server |
| `update_ticket` | Обновление статуса тикета или добавление заметок в историю | Support Server |

## ✅ Task Management Tools (6 инструментов) 🆕

| Инструмент | Описание | Сервер |
|------------|----------|--------|
| `create_task` | Создание новой задачи с заголовком, приоритетом, назначением и связью с тикетом | Support Server |
| `list_tasks` | Список задач с фильтрами (статус, приоритет, исполнитель) | Support Server |
| `update_task` | Обновление статуса задачи, приоритета, исполнителя или добавление заметок | Support Server |
| `get_task` | Получение полной информации о задаче включая историю | Support Server |
| `get_team_workload` | Получение загрузки команды и доступности для умного назначения задач **с поддержкой русских запросов** 🔄 | Support Server |
| `search_similar_tasks` | Поиск похожих задач с использованием семантического поиска | Support Server |

**Новое (2026-02-16):** `get_team_workload` теперь автоматически переводит русские фильтры ролей (например, "разработчик", "босс") в английские эквиваленты ("developer", "boss")

---

## 📊 Итоги

- **Всего инструментов**: 28 🆕
    - RAG/Embeddings: 6 инструментов
    - GitHub MCP: 8 инструментов (включая 2 для ревью PR)
    - Local Git: 4 инструмента
    - Support/CRM: 4 инструмента (День 23)
    - Task Management: 6 инструментов (День 24 🆕)

## 🎯 Как использовать

1. **Поиск в документах**: Используйте `/help <вопрос>` для локального RAG поиска
2. **GitHub операции**: Включите ключевое слово `"GitWithLove"` в ваше сообщение
3. **Локальный Git**: Включите ключевое слово `"GitLocal"` в ваше сообщение
4. **Ревью Pull Request**: Включите ключевое слово `"ReviewPR"` для анализа PR 🆕
5. **RAG инструменты**: Автоматически используются при индексации документов через экран Ollama
6. **Поддержка пользователей**: Используйте экран "Ассистент поддержки" - автоматически создаёт тикеты и ищет решения в FAQ 🎫

## 💡 Примеры запросов

### Ревью кода
```
ReviewPR сделай ревью для PR #45
ReviewPR проверь pull request AI-with-Love#12
код-ревью для PR #8
```

### GitHub операции
```
GitWithLove покажи информацию о репозитории facebook/react
GitWithLove найди код с использованием Compose в android/compose-samples
```

### Локальный Git
```
GitLocal покажи статус репозитория
GitLocal покажи разницу в файлах
```

### Поддержка пользователей
```
Почему не работает авторизация?
Как настроить двухфакторную аутентификацию?
Не могу войти в систему после смены пароля
Какую LLM ты используешь?
Покажи информацию о текущем тикете
```

**Примечание:** Тикеты создаются автоматически при отправке первого сообщения в экране поддержки. Для каждого нового диалога создаётся новый тикет в CRM.

### Управление задачами (Team Assistant) 🆕
```
Создай задачу - Исправить баг авторизации
Покажи задачи с приоритетом high
Покажи список разработчиков
Кто из Backend разработчиков свободен?
Назначь задачу #2 на Наташу Петрову
Покажи все задачи
```

**Поддержка русского языка:** Запросы типа "разработчик", "босс", "backend" автоматически переводятся для поиска членов команды.

## 🗂️ Структура данных

### CRM система
- **Файл**: `server/data/crm_tickets.json` - хранилище тикетов поддержки
- **Файл**: `server/data/crm_users.json` - база пользователей
- **Тикет**: автоинкрементный ID, статус (open/in_progress/resolved/closed), приоритет, категория, история
- **История**: все изменения и заметки с timestamps

### Task Management система 🆕
- **Файл**: `server/data/tasks.json` - хранилище задач
- **Файл**: `server/data/team_members.json` - база членов команды с ролями и навыками
- **Задача**: ID, заголовок, описание, статус (todo/in_progress/done), приоритет (low/medium/high), исполнитель, связь с тикетом
- **Команда**: 3 члена команды (1 Boss, 2 Developers) с навыками и отслеживанием загрузки
- **Языковая поддержка**: роли хранятся на английском, но запросы поддерживаются на русском через автоматический перевод

### RAG база
- **Файл**: `server/data/embeddings.db` - SQLite база с индексированными документами
- **FAQ**: `FAQ.md` - документация для автоматических ответов (52 чанка)
- **Эмбеддинги**: 768-мерные векторы (модель nomic-embed-text)



