package com.example.aiwithlove.util

/**
 * SecureData - Contains sensitive configuration data
 *
 * ⚠️ SECURITY WARNING:
 * This file contains sensitive information and should NEVER be committed to version control.
 * It is listed in .gitignore to prevent accidental commits.
 *
 * For new developers:
 * 1. Copy SecureData.kt.example to SecureData.kt
 * 2. Replace placeholder values with actual Ollama server details
 * 3. Never commit this file to Git
 */
object SecureData {
    /**
     * Ollama Server Configuration
     *
     * DEVELOPMENT SETUP (localhost):
     * - Android Emulator: Use "10.0.2.2" (special emulator IP to reach host machine)
     * - Physical Device: Use your machine's local network IP (e.g., "192.168.1.100")
     *
     * PRODUCTION SETUP (remote server):
     * - Use server's public IP address or domain name
     *
     * Default Ollama port is 11434
     */
    const val SERVER_IP = "10.0.2.2"   // Android emulator → host machine's localhost
    const val SERVER_PORT = 11434      // Default Ollama port

    /**
     * Derived URLs
     */
    val OLLAMA_SERVER_URL: String
        get() = "http://$SERVER_IP:$SERVER_PORT"
}
